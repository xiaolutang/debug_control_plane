{
  "_fixture_meta": {
    "endpoint": "POST 请求 body 非法（非 JSON / 非 object / 空字符串）",
    "contract_ref": "PROTOCOL.md §4.2",
    "source_of_truth": "dart/lib/src/http_codec.dart:27-61 readObject/requiredObject/requiredString + http_sse_transport.dart:88-91",
    "type": "semantic",
    "http_status": 400,
    "notes": "POST 必须带 JSON object body。三个可能的 message 由触发的校验点决定：readObject 解析失败='Request body must be valid JSON object.'；requiredObject 非 object='Expected object.'；requiredString 空串='Expected non-empty string.'。本 fixture 取最常见 readObject 失败路径。code 是框架内置枚举 invalid_request。BF003 归一化时跳过 _ 前缀键",
    "message_variants": [
      "Request body must be valid JSON object.",
      "Expected object.",
      "Expected non-empty string."
    ]
  },
  "ok": false,
  "code": "invalid_request",
  "message": "Request body must be valid JSON object."
}
