---
task_id: R001-BB001
requirement_cycle: R001
domain: business
status: completed
implementer_status: completed
visual_verify_status: not_applicable
simplify_status: completed
simplify_skipped: false
evaluate_status: pass
test_log_status: pass
commit_sha: 607ddc0
subagent_required: true
isolation: direct_subagent
provider: direct_subagent
subagent_role: xlfoundry-implementer
execution_id: xlf-subagent:R001-BB001:implementer:01
subagent_execution_id: xlf-subagent:R001-BB001:implementer:01
degraded_review: false
blocked_stage: null
blocked_reason: ""
---

# R001-BB001 Evidence

## Execution Summary

- mode: negotiated（Mode 1）
- status: completed
- timestamp: 2026-08-23T14:18Z–14:27Z
- run_id: R001-BB001-a1-20260823T141817120Z-aeb24bc4bbc1

## Task Package

任务条目见 `.dev-flow/R001/debug-plane-auth-tasks.md` R001-BB001 节。

## Contract (Mode 1)

`.dev-flow/R001/contracts/R001-BB001.md`（status: agreed）

关键决策：
1. `_bridge_error_to_mcp` 增加 `DeviceAuthError` 分支，置于 `DeviceHttpError` 之前（子类关系），按六个稳定 code 输出三类可操作提示，不含明文 token。
2. `CapabilityMirror.refresh` 对 auth error 不与 offline degrade 混淆——不清 schema 缓存、不误报 change，记录 per-device auth 状态；refresh 保持 poll 安全，由 `h_list_capabilities` 主动检查并抛 MCP isError。
3. `h_subscribe_events` 建连 401/403 经 `_bridge_error_to_mcp` 返回 MCP isError，不崩溃；既有 offline/stale/transport 行为不变。
4. 范围外：不改 bridge_client.py / DevicePool / 跨语言代码；不新增 MCP tool。

## Implementation Result

- TDD red→green（先写 18 个测试，15 失败后实现）。
- `server.py`：
  - `_bridge_error_to_mcp` 新增 `DeviceAuthError` 分支（server.py:190，先于 DeviceHttpError:200）。
  - 模块级常量表 `_AUTH_CODE_ACTIONS`（server.py:159-170）覆盖 authorization_required / token_expired / token_revoked / invalid_token / authorization_denied / forbidden，与 bridge_client.py `_AUTH_ERROR_CODES` 一致；未知 code 回退 deny-style hint（不自动重试的安全默认）。
  - `h_list_capabilities` 的 `_probe` 返回 `(schemas, changed, auth_err)`，auth_err 非空时 raise `_bridge_error_to_mcp` → MCP isError，不静默降级为空列表（server.py:375-383）。
  - `h_subscribe_events` 既有 catch 天然覆盖 `DeviceAuthError`（BridgeError 子类）→ MCP isError 不崩溃。
- `capability_mirror.py`：
  - `refresh` 新增 `DeviceAuthError` 专用 except 分支（:445-451）：不清 `_cache`、返回 False、记录 `_auth_errors`；offline/stale/HTTP 分支维持原 degrade 且清除 auth 状态；成功 refresh 清除 auth 状态。
  - 新增 `auth_error(device_id)` 查询接口。
- simplify 收敛：if/elif 链收敛为常量表 `_AUTH_CODE_ACTIONS`，行为与消息文本完全不变。
- 范围外文件（bridge_client.py / device_pool.py / 跨语言）零改动。

## Quality Gates

- **simplify_status**: completed
- **simplify_skipped**: false
- **evaluate_status**: pass

> simplify 6 视角审查完成，1 处收敛（常量表化），346 测试全绿验证等价。
> evaluate verdict=pass，Mode 1 Contract agreed 且实现一致，hard gates 6/6 PASS，scored criteria 19/19（A 级）。

## 测试日志

- `.dev-flow/R001/evidence/R001-BB001-unit-backend-test.log`（focused 100 passed；全量 346 passed / 2 skipped，exit 0）

## Evaluation Result Detail

- verdict: pass
- contract_compliance: agreed 且实现一致（isinstance 顺序 / 六 code 映射 / refresh 不清缓存 / poll 安全 / h_list_capabilities 抛 isError / 范围外零改动，逐项核对代码行号）
- acceptance_results:
  - `_bridge_error_to_mcp` auth error 可操作提示 — PASS（六 code 三类提示；test_server.py:342-388 覆盖无明文 token 与分支顺序）
  - `list_capabilities` 不静默降级 — PASS（auth_err 非空抛 McpError；offline 场景回归仍空列表）
  - `subscribe_events` 401/403 返回 isError 不崩溃 — PASS（test_server.py:433-448）
- scored_criteria: completeness=5, correctness=5, contract_alignment=5（total 19/19，等级 A）
- next_action: null

## Commit

- sha: 607ddc0
- message: feat(python): R001-BB001 MCP auth error surfacing [impl:claude eval:claude] xlfoundry:v5.15.0
