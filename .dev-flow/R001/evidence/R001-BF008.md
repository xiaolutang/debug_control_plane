---
task_id: R001-BF008
requirement_cycle: R001
domain: backend
status: completed
implementer_status: completed
visual_verify_status: not_applicable
simplify_status: completed
simplify_skipped: false
evaluate_status: pass
test_log_status: pass
commit_sha: ad8ba7b
subagent_required: true
isolation: direct_subagent
provider: direct_subagent
subagent_role: xlfoundry-implementer
execution_id: xlf-subagent:R001-BF008:implementer:01
subagent_execution_id: xlf-subagent:R001-BF008:implementer:01
degraded_review: false
blocked_stage: null
blocked_reason: ""
---

# R001-BF008 Evidence

## Execution Summary

- mode: direct
- status: completed
- timestamp: 2026-08-23T13:48:06Z（implementer 开始）/ 2026-08-23T13:54:37Z（evaluate 结束）
- run_id: R001-BF008-a1-20260823T134739748Z-972d5f38de53

## Task Package

任务条目见 `.dev-flow/R001/debug-plane-auth-tasks.md` R001-BF008 节。

## Contract (Mode 1)

null（mode=direct，Mode 2 直连验收）

## Implementation Result

- 新建 `dart/test/control_plane_auth_test.dart`：
  - `_FakeAuthManager` 可配置 fake（authorizeDecision / helloState / request|status|claimResult，记录 authorizeCalls）。
  - 通过真实 `HttpSseTransport + ControlPlane` 栈覆盖 6 个 auth fixtures：hello-auth-required（未授权 hello 最小 bootstrap，无 state/capability 泄漏）、hello-auth-authorized（Bearer 假 token 完整 hello + auth 状态）、error-401-authorization-required、error-401-token-expired、error-403-authorization-denied、auth-claim-approved。
  - SSE：未授权 `/events` 在首个 SSE 帧前返回 JSON 401（application/json、无 `: connected` 前导帧、subscriberCount==0）；授权 `/events` 首帧与 `sse-connected.bin` 字节一致。
  - `authManager == null` 时 bare `/hello` 无 auth 键。
- 修改 `dart/test/golden_fixture_test.dart`：
  - 新增 auth fixtures 存在性 + closed-set unstable reason 守卫、claim token 假值守卫。
  - simplify 收敛：删除与既有 hello.json 用例重复的 bare-mode 测试；提取 `_unstableReasons()` 共享 helper 消除三份 walk 闭包复制；移除 `_httpGet/_httpPost` 死返回值分量。
- 测试 token 均为 `test-token-not-real` 明显假值；未修改 dart/lib 生产代码；fixtures/ 无改动。

## Quality Gates

- **simplify_status**: completed
- **simplify_skipped**: false
- **evaluate_status**: pass

> simplify 6 视角审查完成，3 处收敛（冗余用例删除 / helper 提取 / 死返回值移除），收敛后 101 tests passed。
> evaluate verdict=pass，acceptance_criteria 3/3 通过，scored_criteria completeness=5 correctness=5 protocol_alignment=5。

## 测试日志

- `.dev-flow/R001/evidence/R001-BF008-unit-flutter-test.log`（`cd dart && fvm flutter test`，101 tests passed）

## Evaluation Result Detail

- verdict: pass
- acceptance_results:
  - Dart tests 覆盖新增 auth fixtures — pass（control_plane_auth_test.dart L136-269 覆盖全部 6 个 auth fixtures，normalizedEquals 全量比对）
  - `cd dart && fvm flutter test` 通过 — pass（日志末行 `00:00 +101: All tests passed!`）
  - 未启用 auth 的既有 golden fixture 不变 — pass（git status fixtures/ 干净；既有 golden 用例全过；新增 bare-mode no-auth-keys 守卫）
- scored_criteria: completeness=5, correctness=5, protocol_alignment=5（与 Kotlin ControlPlaneAuthTest 场景/fixture 语义镜像；经真实 HTTP 栈而非 mock）
- next_action: null

## Commit

- sha: ad8ba7b
- message: test(dart): R001-BF008 auth golden alignment tests [impl:claude eval:claude] xlfoundry:v5.15.0
