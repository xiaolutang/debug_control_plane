---
task_id: R004-BF002
requirement_cycle: R004
status: implemented
implementer_status: completed
simplify_status: completed
evaluate_status: pass
visual_verify_status: not_applicable
test_log_status: deferred
commit_gate_status: unchecked
blocked_stage: null
blocked_reason: ""
domain: backend
subagent_required: true
isolation: direct_subagent
provider: direct_subagent
subagent_role: xlfoundry-implementer
execution_id: xlf-subagent:R004-BF002:implementer:01
implementer_execution_id: xlf-subagent:R004-BF002:implementer:01
---

# R004-BF002 Implementation Evidence

验收脚本 install -r 改造 + 端到端 runner（design §3.4 / DEC-R004-004）。纯验收层任务：不触碰 python 包源码 / dart / kotlin。真机不在场，android 集成 6 用例按 deferred 契约登记（device_required），cross_stack 断言链真实执行且 PASS。

## Files Changed

- `.dev-flow/R004/test-overrides/R004-BF002/integration-android.sh`（新建，fork 自 R003-BF008，只读不回写）
  - 无条件 uninstall 移除：仅 `DELETE_AND_REINSTALL=1` 执行（逃生门，等价首次授权，用例 6 依赖）
  - 安装统一 `install -r`（`install_apk() { adb_cmd install -r "$APK_PATH"; }`）
  - `APK_ALREADY_INSTALLED` 快速路径保留，注释写明两个机制分工：安装弹窗（HyperOS「USB 安装」授权）靠「APK 未变不装」压制；auth 弹窗靠 token 持久化消灭——两件事
  - 头部注释与 test-log 执行路径描述同步为 R004 路径/语义（log 描述含「仅 DELETE_AND_REINSTALL=1 时 uninstall 逃生门 → install -r → am start -W → python e2e runner 6 用例」）
  - 三阶段 page-scope 采集段替换为 python runner 调用（设备无 curl 通道，断言 python 侧发起）
- `.dev-flow/R004/test-overrides/R004-BF002/e2e-token-persistence.py`（新建 runner）
  - 6 用例：case1 首次授权双侧落盘（auth request→claim 链或复用已落盘 token；tokens.json + 设备侧 `files/debug_control_plane/debug_auth_tokens.json` run-as 检查；expiresAt≈7d）/ case2 冷重启 force-stop+start → 旧 token Bearer /hello 200 [device] / case3 install -r 主断言 [device] / case4 python 重启免 auth（子进程真实读盘 get_token）/ case5 过期自动重授权（构造过期行 → get_token=None → 授权链可达，用后还原）/ case6 清装逃生门（DELETE_AND_REINSTALL=1 → 401 invalid_token）[device]
  - deferred 契约：`adb devices` 检测，不在场 → device 用例 deferred(device_required)，exit 0；4/5 视可达性 skip(setup_required)
  - cross_stack 断言链始终执行：bearer-reuse（新 provider 实例真实读盘命中 Bearer 头）/ 401-clear-relay（401 token_expired → clear_token → 新实例读盘确认行删）——mock 仅传输层（httpx MockTransport），token 读路径不 mock
- `.dev-flow/R004/evidence/`（evidence 双写）
  - `R004-BF002-integration-android-test.log`（status: deferred / deferred_reason: device_required）
  - `token-persistence-e2e.md`（空壳结构，6 用例分节占位）
  - `R004-BF002-integration-cross_stack-test.log`（真实执行，PASS）

## Tests

- command: `bash .dev-flow/R004/test-overrides/R004-BF002/integration-android.sh`（本任务 test_tasks 声明 android+cross_stack 两个 integration 域，android 域由本脚本承担，cross_stack 域由内嵌 runner 承担并落独立 log）
- 实测（本次运行，真机不在场）：
  - deferred 路径验证：`adb devices` 空 → DEFERRED 输出 + evidence 双写 + exit 0（非失败）
  - cross_stack：bearer-reuse PASS；401-clear-relay PASS（首轮 FAIL：mock 错误体误用 `errorCode` 字段，wire 契约实为 `code`（bridge_client._auth_error_code），已修正对齐）
  - unit 层无新增（纯验收层任务，test_tasks 未声明 unit）

## Acceptance Check

- fork R003-BF008 脚本且不回写 R003 产物: passed（.dev-flow/R003/ 零改动，git status 可证）
- 无条件 uninstall 移除（仅 DELETE_AND_REINSTALL=1）且安装统一 install -r: passed
- APK_ALREADY_INSTALLED 快速路径保留且注释写明安装弹窗/auth 弹窗机制分工: passed
- 头部注释与 test-log 执行路径描述同步: passed
- python runner 覆盖 6 用例: passed（case2/3/6 标 device-deferred）
- deferred 时 evidence 双写空壳（device_required）沿用 R003 模式: passed（实测）
- 真机在场时 1/2/3/6 必须 pass: deferred（待设备回收；runner 判定逻辑：case1/2/3 非 pass 即 fail，case6 逃生门轮次外显式 deferred）

## Summary

fork 改造完成且 deferred 路径实测通过；cross_stack 断言链真实 PASS。真机 6 用例待设备（Xiaomi 23116PN5BC）可用时重跑 `bash .dev-flow/R004/test-overrides/R004-BF002/integration-android.sh` 回收（用例 6 需 `DELETE_AND_REINSTALL=1` 单独轮次）。

## Structured Summary

```json
{
  "task_id": "R004-BF002",
  "status": "success",
  "files_changed": [
    ".dev-flow/R004/test-overrides/R004-BF002/integration-android.sh",
    ".dev-flow/R004/test-overrides/R004-BF002/e2e-token-persistence.py",
    ".dev-flow/R004/evidence/R004-BF002-integration-android-test.log",
    ".dev-flow/R004/evidence/R004-BF002-integration-cross_stack-test.log",
    ".dev-flow/R004/evidence/token-persistence-e2e.md",
    ".dev-flow/R004/evidence/R004-BF002.md"
  ],
  "test_results": {
    "unit": "skipped（test_tasks 未声明 unit）",
    "integration": "cross_stack PASS；android deferred（device_required，真机不在场）"
  },
  "simplify_result": {"status": "pending", "issues_found": 0, "issues_fixed": 0},
  "architecture_md_updated": {"has_architecture_change": false, "updated": "no_change_needed"},
  "evaluate_ready": true,
  "commit": null,
  "summary": "fork R003-BF008 脚本改 install -r 语义（uninstall 仅逃生门+弹窗分工注释），新建 6 用例 e2e runner，deferred 路径实测通过，cross_stack 断言链 PASS，真机用例待回收"
}
```

## Simplify Result

- reviewed_files:
  - .dev-flow/R004/test-overrides/R004-BF002/integration-android.sh
  - .dev-flow/R004/test-overrides/R004-BF002/e2e-token-persistence.py
  - .dev-flow/R004/evidence/R004-BF002-integration-android-test.log
  - .dev-flow/R004/evidence/R004-BF002-integration-cross_stack-test.log
  - .dev-flow/R004/evidence/token-persistence-e2e.md
  - 对照: .dev-flow/R003/test-overrides/R003-BF008/integration-android.sh、R003 evidence 同名产物
- fixed: []（无收敛级修改）
- routing: none
- bounced_to_implementer: []
- residual_risks:
  - case1 python 侧断言按 tokens.json 首行键取值（json.loads 后 next(iter)），与 ensure_token 落盘的 "__e2e__" 行存在键序耦合，极端键序下可能误判；真机回收轮次实测确认即可，不构成回退
  - case5 直接改写 ~/.debug-control-plane/tokens.json 真实行（用后还原）；若运行中途中断会留下人为过期行，回收时留意
  - .android-work 工作目录未入 .gitignore（R003 同构未忽略，非本任务收敛范围）
- agents_summary:
  - agent_1_reuse: {issues_found: 0, issues_fixed: 0}
  - agent_2_quality: {issues_found: 0, issues_fixed: 0}
  - agent_3_efficiency: {issues_found: 0, issues_fixed: 0}
  - agent_4_architecture: {issues_found: 0, issues_fixed: 0, skipped: false}
  - agent_5_test: {issues_found: 0, issues_fixed: 0, bounced: false}
  - agent_6_design: {issues_found: 0, issues_fixed: 0, skipped: false}

### SubagentExecutionEvidence

- isolation: independent_context
- provider: direct_subagent
- subagent_role: xlfoundry-simplify
- execution_id: xlf-subagent:R004-BF002:simplify:01

### 六视角审查记录

1. 代码复用：fork 保留了 R003-BF008 的 get_package_name/aapt 校正、resolve-activity 校验、HyperOS 重试引导等既有 helper；runner 复用 BridgeClient/FileTokenProvider/DevicePool 现成实现，未新造重复工具层。无问题。
2. 代码质量：diff 逐块语义对位（uninstall 逃生门重排为三分支 elif 链、install -r 统一、步骤编号 5→6、[n/6] 全部一致）；两个弹窗压制机制分工在头注与步骤 2 注释双处一致；无字符串驱动/参数蔓延。
3. 效率：APK_ALREADY_INSTALLED 快速路径双跳过（install+build）保留；deferred 分支 cross-stack 链始终执行且 `|| true` 不阻断 evidence 双写；case4 子进程一次性探测，无重复轮询。无热路径膨胀。
4. 架构：纯验收层（test-overrides + evidence），未触碰 python 包源码/dart/kotlin；`.dev-flow/R003/` 零改动（git status 无 R003 条目）；跨层断言仅经 BridgeClient 公开 API，未越权威边界。
5. 测试：6 用例与 test.md §3 / acceptance-spec BF002 节逐条对齐；deferred 契约两路可达（设备不在场：case1/2/3/6 deferred + 4/5 可达性 skip + cross-stack 真实执行，本次实测 exit 0；设备在场：case1/2/3 非 pass 即 fail，case6 逃生门轮次外显式 deferred）；cross-stack 链本轮复跑 PASS（bearer-reuse / 401-clear-relay）。断言真实（401 三码集合、expiresAt≈7d±600s、401 invalid_token 主断言），无无效断言或过度 mock（MockTransport 仅传输层，token 读路径真实读盘）。
6. 设计一致性：acceptance-spec BF002 script_contract 五条逐条满足——仅 DELETE_AND_REINSTALL=1 时 uninstall ✓（步骤 2 elif 链）；统一 install -r ✓（install_apk 定义）；快速路径保留 + 机制分工注释 ✓（头注+步骤 2 注）；fork 不回写 R003 ✓；deferred evidence 双写沿用 R003 模式 ✓（frontmatter/空壳结构/回收指引同构）。wire 契约 errorCode→code 修正与 bridge_client._auth_error_code 对齐，已由 PASS 证据背书。

## Sprint Evaluation
- task: R004-BF002
- domain: backend
- mode: negotiated
- status: pass
- grade: A

### Contract Validation
- status: agreed
- path: .dev-flow/R004/analysis/2026-08-31--token-persistence-acceptance-spec.yaml（BF002 节：6 e2e 用例 + script_contract 5 条）

### Hard Gates
| Gate ID | 名称 | 结果 | 证据 |
|---------|------|------|------|
| domain_prefix_consistency | domain/前缀一致性 | PASS | BF002 验收脚本 + runner，纯 backend/python 验收层，无 ui 载体；前缀 BF 与 domain 一致 |
| integration_test_executed | 集成测试执行 | PASS | android 域 deferred（deferred_reason: device_required，白名单内，R004-BF002-integration-android-test.log）；cross_stack 域真实 pass（exit 0，bearer-reuse/401-clear-relay 双 PASS） |
| script_contract_met | script_contract 5 条逐条 | PASS | 见下方矩阵（逐条对照 integration-android.sh） |
| r003_untouched | 不回写 R003 产物 | PASS | `git status .dev-flow/R003/` 零条目（实测）；脚本文案注明 fork 自 R003-BF008 |
| six_case_coverage | 6 用例覆盖 | PASS | e2e-token-persistence.py case1–case6 函数齐全，与 spec BF002 e2e 节逐条对位；case2/3/6 标 [device,deferred] |
| deferred_contract_valid | deferred 契约 | PASS | deferred_reason=device_required 在白名单；evidence 双写（android log deferred + token-persistence-e2e.md 空壳 6 分节 + 回收指引），沿用 R003 模式同构 |
| simplify_pass | simplify 完成 | PASS | evidence Simplify Result 节：六视角 0 bounce，residual_risks 已登记 |
| acceptance_criteria_met | 验收标准逐条 | PASS | 见下方矩阵（7/7，其中 #7 为 deferred 合法） |

### Acceptance Criteria Checklist
| # | acceptance_criteria | 验证方式 | 结果 | 证据 |
|---|-------------------|---------|------|------|
| 1 | fork R003-BF008 脚本且不回写 R003 产物 | 代码审查 + git | PASS | integration-android.sh:3 注明 fork；`git status .dev-flow/R003/` 0 条目 |
| 2 | 无条件 uninstall 移除（仅 DELETE_AND_REINSTALL=1）且统一 install -r | 代码审查 | PASS | integration-android.sh:203-213（elif 三分支：逃生门 uninstall / 已装快速路径 / 未装 install -r）；:236 `install_apk() { adb_cmd install -r ...}` |
| 3 | APK_ALREADY_INSTALLED 快速路径保留 + 弹窗机制分工注释 | 代码审查 | PASS | :14-19 头注双机制分工；:196-201 步骤 2 重复强调；:207-209 快速路径跳过 install |
| 4 | 头部注释与 test-log 执行路径描述同步 | 代码审查 | PASS | :312-318 真机 log 执行路径含「仅 DELETE_AND_REINSTALL=1 时 uninstall 逃生门 → build → install -r → am start -W → python e2e runner 6 用例」，与头注语义一致 |
| 5 | python runner 覆盖 6 用例 | 代码审查 | PASS | e2e-token-persistence.py case1(:301)/case2(:332)/case3(:343)/case4(:355)/case5(:386)/case6(:426)；case2/3/6 device-deferred(:499) |
| 6 | deferred 时 evidence 双写空壳（device_required）沿用 R003 模式 | 脚本实测 + log 审查 | PASS | write_deferred_evidence(:77-161) 双写 android log + 空壳 md；本次运行实测产出两份 deferred 产物 |
| 7 | 真机在场时 1/2/3/6 必须 pass | 代码审查（判定逻辑） | PASS(逻辑) / deferred(执行) | overall_status(:458-471)：真机在场时 case1/2/3 非 pass 即 fail；case6 由 DELETE_AND_REINSTALL 轮次覆盖；真机本轮不在场，按 deferred 契约登记待回收 |

### Scored Criteria
| 维度 | 分数(1-5) | 阈值 | 通过 | 说明 |
|------|----------|------|------|------|
| 脚本正确性 | 5 | 3/5 | YES | 三分支安装语义、逃生门、快速路径、双写结构均与契约逐条对位 |
| 断言质量 | 5 | 3/5 | YES | cross_stack 真实读盘（MockTransport 仅传输层）；401 三码集合、expiresAt≈7d±600s、invalid_token 主断言真实有效 |
| deferred 契约工艺 | 5 | 3/5 | YES | 双写空壳 + 回收指引 + exit 0 语义 + case6 单独轮次说明，与 R003 模式同构 |
| 测试覆盖 | 4 | 3/5 | YES | 6 用例齐全；case1 键序耦合与 case5 改写真实行风险已由 simplify 登记，待真机轮次确认 |

### 总分: 19/20 | 等级: A

### Findings
- [testability] [low] case1 python 侧断言取 tokens.json 首行键（next(iter)），与落盘行存在键序耦合，极端键序可能误判（simplify 已登记，真机回收轮次实测确认）
- [hygiene] [low] case5 直接改写 ~/.debug-control-plane/tokens.json 真实行（用后还原），运行中断会留下人为过期行
- [hygiene] [low] .android-work 工作目录未入 .gitignore（R003 同构未忽略，非本任务收敛范围）

### Next Action
- ready_for_commit（真机 6 用例为 deferred 债务，待设备 Xiaomi 23116PN5BC 可用时回收）

### Evaluator Evidence
- isolation: independent_context
- provider: direct_subagent
- subagent_role: xlfoundry-evaluate
- execution_id: xlf-subagent:R004-BF002:evaluate:01
