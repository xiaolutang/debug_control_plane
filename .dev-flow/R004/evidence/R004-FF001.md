---
task_id: R004-FF001
requirement_cycle: R004
status: implemented
implementer_status: completed
simplify_status: completed
evaluate_status: pass
visual_verify_status: UNCERTAIN
test_log_status: pass
commit_gate_status: unchecked
blocked_stage: null
blocked_reason: ""
visual_deferred: true
visual_deferred_reason: device_required
visual_baseline_owner: R004-BF002
domain: ui
subagent_required: true
isolation: direct_subagent
provider: direct_subagent
subagent_role: xlfoundry-implementer
execution_id: xlf-subagent:R004-FF001:implementer:02
implementer_execution_id: xlf-subagent:R004-FF001:implementer:02
---

# R004-FF001 Implementation Evidence

app 侧 FileBackedPluginDebugAuthStore — token 持久化与 attach 惰性升级接线（design §3.1 / DEC-R004-001）。

## Files Changed

- `flutter_debug_control_plane/android/src/main/kotlin/com/pantas/debug/controlplane/flutter/PluginDebugAuth.kt`
  - 新增 `FileBackedPluginDebugAuthStore`（装饰器）：主构造接收目录 `File`（纯 JVM 可测）+ `Context` 便捷构造（`filesDir/debug_control_plane/debug_auth_tokens.json`）；`InMemoryPluginDebugAuthStore` 增加 `snapshotTokens()` 快照口
  - pending 组 3 方法纯透传（红线：tokenPlaintext/pairingCode 绝不落盘）；token 组写方法（putToken/markRevoked/markAllRevoked）透传后在 `persistLock` 临界区内重收集快照并 tmp+rename 原子写
  - load：懒加载一次；损坏（JSON 解析/IO/未知 version）→ warn 回退空不抛；`expiresAt < now` 记录丢弃并回写；persist IO 失败 warn 不回滚内存
- `flutter_debug_control_plane/android/src/main/kotlin/com/pantas/debug/controlplane/flutter/DebugControlPlaneFlutterPlugin.kt`
  - `onAttachedToEngine` 惰性升级（方案 A）：首次 attach 且未被 host 注入时，用 `binding.applicationContext` 将 companion `processAuthStore` 幂等升级为 FileBacked（复用原 InMemory 工作集 = 内存记录零丢失迁移）；`setAuthStoreForHost` 整体替换语义不变并抑制升级
- `flutter_debug_control_plane/android/src/test/kotlin/com/pantas/debug/controlplane/flutter/PluginDebugAuthStorePersistenceTest.kt`（新增，9 用例）

## Tests

- command: `bash scripts/run-tests.sh R004-FF001 unit android`（等价 `cd flutter_debug_control_plane/android && ../../kotlin/gradlew -p . testDebugUnitTest`）
- log: `.dev-flow/R004/evidence/R004-FF001-unit-android-test.log`
- result: **pass — 79 tests, 0 failures, 0 errors**（新增 9 + 既有回归 70）

新增用例（8 场景，9 test 方法）：
1. roundtrip：token 记录跨 store 重启往返一致
2. corrupt-fallback：截断 JSON / 未知 version → 回退空不抛，且后续写自愈
3. nullable-roundtrip：revokedAt/clientLabel null 与非 null 双向往返
4. revoked-persist：markRevoked / markAllRevoked 落盘后重启可见
5. expired-cleanup：过期记录 load 时丢弃且回写（文件不再含该 tokenId）
6. atomic-write：25 次写后目录无 tmp 残留，目标文件始终完整可解析
7. concurrent-persist：8 线程 × 25 写 + 交错 markRevoked，最终文件解析出完整一致 200 条状态
8. plaintext-redline：含明文 token 的 pending 落 store 后，文件内容不含明文 token 子串、不含 pairingCode、不含 requestId，仅含 hash

## Acceptance Check

- 装饰器内嵌 InMemory 工作集且 pending 纯透传不落盘: passed
- putToken/markRevoked/markAllRevoked 同步原子写（tmp+rename）且临界区重收集快照: passed
- load 损坏回退空不抛且过期记录丢弃并回写: passed
- onAttachedToEngine applicationContext 幂等升级 + 内存记录迁移（复用同一 delegate 工作集）: passed
- setAuthStoreForHost 整体替换语义不变: passed
- 单测覆盖 8 场景（9 用例含回归）: passed

## Deviations

- 迁移实现方式：设计要求「迁移内存记录」——实现采用**复用原 InMemory 作为 FileBacked 的 delegate 工作集**，记录天然零拷贝迁移（比逐条复制更安全，无窗口丢写）；类上另留 `migrateFrom`（foreign store 防御路径，接口无枚举 API 时仅记 warn 不迁移）
- 主构造为 `File`（目录）而非 Context：满足任务包「测试注意」关键设计（纯 JVM @TemporaryFolder 构造，无 android 依赖），Context 为次构造工厂形态
- persist 文件位于 `filesDir/debug_control_plane/` 子目录（与主构造目录语义一致；Context 路径 = `<filesDir>/debug_control_plane/debug_auth_tokens.json`）

## Visual Verify

device_required，deferred 至 R004-BF002（真机覆盖安装 + 旧 token 复用端到端验收）。

### Visual Verify Execution (2026-08-31)

- verdict: **DEFERRED（device_required）**
- 判定依据：
  1. `adb devices` 输出为空（目标设备 23116PN5BC 不在场），无法执行真机安装 + 运行时视觉证据采集；按降级原则不伪造视觉验证，输出非 PASS。
  2. deferred 登记完整：frontmatter 含 `visual_verify_status: UNCERTAIN` / `visual_deferred: true` / `visual_deferred_reason: device_required` / `visual_baseline_owner: R004-BF002`，与 R003-FF001 同类先例模式一致（视觉验证随 e2e 任务真机在场时回收）。
  3. diff 快速核对：`git diff --stat` 显示改动仅限 `PluginDebugAuth.kt`（+212，新增 FileBackedPluginDebugAuthStore 装饰器）、`DebugControlPlaneFlutterPlugin.kt`（attach 接线，+31/-x）、测试文件与 `.dev-flow` 流程文件——**无 layout XML、无 Dart/UI 代码改动**，本任务为纯存储层改动，无新 UI 表现可验收。
  4. 唯一相关稳定标识 `acceptance.auth.token_status_text` 属 R004-BF002 端到端范围，归 baseline owner（R004-BF002）在真机在场时回收。

SubagentExecutionEvidence:
- isolation: independent_context
- provider: direct_subagent
- subagent_role: xlfoundry-visual-verify
- execution_id: xlf-subagent:R004-FF001:visual-verify:01

## Simplify Result

- reviewed_files:
  - flutter_debug_control_plane/android/src/main/kotlin/com/pantas/debug/controlplane/flutter/PluginDebugAuth.kt
  - flutter_debug_control_plane/android/src/main/kotlin/com/pantas/debug/controlplane/flutter/DebugControlPlaneFlutterPlugin.kt
  - flutter_debug_control_plane/android/src/test/kotlin/com/pantas/debug/controlplane/flutter/PluginDebugAuthStorePersistenceTest.kt
- fixed:
  - file: flutter_debug_control_plane/android/src/main/kotlin/com/pantas/debug/controlplane/flutter/PluginDebugAuth.kt
    description: 删除零调用且语义混乱的 public `migrateFrom(other)`（foreign-store 分支只 log 不迁移，留下永远不会被用的接口口子），收敛为语义准确的 `persistNow()`（仅落盘当前工作集）
  - file: flutter_debug_control_plane/android/src/main/kotlin/com/pantas/debug/controlplane/flutter/DebugControlPlaneFlutterPlugin.kt
    description: attach 升级路径补一行 `(authStore as? FileBackedPluginDebugAuthStore)?.persistNow()`——升级仅 load 磁盘到工作集，从不把升级前已有的内存 token 冲写落盘；加注释明确零拷贝迁移
- routing: none
- bounced_to_implementer: []
- residual_risks:
  - Kotlin 装饰器仅在单进程单 store 实例下串行化（store 绑进程、companion 唯一实例，当前架构成立）；无外部写者
  - `authUpgraded` 为实例字段：同一 plugin 实例先 `setAuthStoreForHost` 再 detach/attach 会回落到 `processAuthStore`（host 注入语义本身即每次 attach 需重注入，既有行为，非本 diff 引入）
  - Java 桌面 / 纯 JVM 宿主（无 android Context attach 路径不走 upgrade）维持 InMemory，行为与 R004 前一致，符合设计范围（非目标：纯 Dart 路径）
- agents_summary:
  - agent_1_reuse: {issues_found: 1, issues_fixed: 1}  # 删除死代码 migrateFrom；复用 org.json/InMemory delegate，零新依赖
  - agent_2_quality: {issues_found: 1, issues_fixed: 1}  # 死 public API + 误导性 KDoc；手写 JSON 编码边界（转义/NULL/ISO-8601 Instant.toString↔parse 往返）确认正确，测试用例 8 显式覆盖红线
  - agent_3_efficiency: {issues_found: 1, issues_fixed: 1}  # 升级路径漏掉首写（内存→磁盘 flush 缺失）；persist 全量重写为设计约定（记录数级为个位数~几十，可接受）
  - agent_4_architecture: {issues_found: 0, issues_fixed: 0, skipped: true}  # architecture.md 不存在，按降级策略跳过；红线核对通过：kotlin/ 与 python/ 零改动、persist 数据源仅 snapshotTokens()、pending 组纯透传
  - agent_5_test: {issues_found: 0, issues_fixed: 0, bounced: false}  # 9 用例无冗余、无泄漏（TemporaryFolder 隔离）、命名清晰、覆盖损坏/未知版本/null 往返/过期/原子/并发/红线；并发用例（8×25 线程）+ 红线用例价值高
  - agent_6_design: {issues_found: 0, issues_fixed: 0, skipped: false}  # 设计 §3.1 逐条核对：装饰器形态/pending 透传红线/tmp+rename/临界区重收集快照/懒加载损坏回退空/过期回写/方案 A attach 惰性升级/setAuthStoreForHost 语义不变/子目录路径全部符合；implementer 报告的 3 项偏差均为合理收敛而非偏离

### Simplify SubagentExecutionEvidence

- isolation: independent_context
- provider: direct_subagent
- subagent_role: xlfoundry-simplify
- execution_id: xlf-subagent:R004-FF001:simplify:01

### Simplify 验证

- command: `bash scripts/run-tests.sh R004-simplify-FF001 unit android`
- log: `.dev-flow/R004/evidence/R004-simplify-FF001-unit-android-test.log`
- result: **pass — 79 tests, 0 failures, 0 errors**（含 PluginDebugAuthStorePersistenceTest 9/9，修改后全绿）

## Sprint Evaluation

- task: R004-FF001
- domain: ui
- taskLayer: foundation
- mode: negotiated（对标 acceptance-spec.yaml FF001 节 + design §3.1）
- status: pass
- grade: A
- evaluated_at: 2026-08-31
- execution_id: xlf-subagent:R004-FF001:evaluate:01

### Contract Validation

- status: agreed（acceptance-spec.yaml FF001 节，见 token-persistence-tasks.md R004-FF001 条目，sourceCapabilities: [FF001]）
- path: .dev-flow/R004/token-persistence-tasks.md（任务包 acceptance_criteria 6 条为验收对标）

### Hard Gates

| Gate ID | 名称 | 结果 | 证据 |
|---------|------|------|------|
| domain_prefix_consistency | domain/前缀一致性 | PASS | FF 前缀 / ui domain；改动为 Flutter plugin android 侧 Kotlin 存储层，属 app（ui domain）载体，无 backend 路由/服务端代码 |
| unit_test_pass | 单测通过 | PASS | evaluate 独立复跑 `testDebugUnitTest`：79 tests, 0 failures, 0 errors（XML 汇总），含 PluginDebugAuthStorePersistenceTest 9/9；与 evidence 声明一致 |
| test_coverage | 测试覆盖三维度 | PASS | 场景覆盖 8/8（roundtrip/损坏回退/可空往返/revoked 持久化/过期清理/原子写/并发 persist/明文红线，测试文件逐条对应）；隔离：@Rule TemporaryFolder 每用例独立目录 + 注入 `now` 时钟，无跨用例残留；过程验证：断言直接读落盘文件内容（tokenFile(dir).readText()）验证中间状态，非仅返回值 |
| protocol_untouched | 协议未动 | PASS | diff 仅 PluginDebugAuth.kt / DebugControlPlaneFlutterPlugin.kt / 新测试文件 + .dev-flow 流程文件；无 ChannelProtocol / 协议消息改动 |
| blocked_files_clean | 红线目录无 diff | PASS | `git status -- kotlin/ python/` 输出为空；DebugAuth.kt（kotlin/ 侧同名协议文件）无改动 |
| plaintext_redline | persist 只写 hash、pending 不落盘 | PASS | PluginDebugAuth.kt:207-215 persistLocked 数据源仅 `delegate.snapshotTokens()`（DebugAuthTokenRecord：tokenId/tokenHash/timestamps/label，无明文字段）；pending 组 3 方法（PluginDebugAuth.kt:127-135）纯透传；测试用例 8 显式断言文件不含 plaintext/pairingCode/requestId |
| attach_idempotent | attach 幂等升级 | PASS | DebugControlPlaneFlutterPlugin.kt:62-69 `upgradeProcessStore` @Synchronized + `current !is InMemoryPluginDebugAuthStore` 双重防重入；:81-95 `authUpgraded` 实例标志 + companion var 全局单次升级 |
| simplify_pass | simplify 完成 | PASS | simplify_status: completed；simplify 后复跑 `testDebugUnitTest` 79/0/0（R004-simplify-FF001-unit-android-test.log）；agents_summary 6 agents（1 skipped: architecture.md 不存在按降级策略） |
| acceptance_criteria_met | 验收标准逐条校验 | PASS | 见下方矩阵（6/6 PASS） |
| visual_deferred_valid | DEFERRED 登记完整 | PASS | frontmatter 4 字段齐全：visual_verify_status: UNCERTAIN / visual_deferred: true / visual_deferred_reason: device_required（白名单内）/ visual_baseline_owner: R004-BF002；与 R003-FF001 先例模式一致；本任务纯存储层无 UI 表现，唯一稳定标识 token_status_text 归 BF002 |

### Acceptance Criteria Checklist

| # | acceptance_criteria | 验证方式 | 结果 | 证据 |
|---|-------------------|---------|------|------|
| 1 | 装饰器形态内嵌 InMemory 工作集且 pending 组纯透传不落盘 | 代码审查 + 测试运行 | PASS | PluginDebugAuth.kt:101-135：`FileBackedPluginDebugAuthStore(…, delegate: InMemoryPluginDebugAuthStore)`；pending/pendingByNonceHash/putPending 三方法一行透传 delegate；测试 `pending records with plaintext token never reach the file`（9/9 通过） |
| 2 | putToken/markRevoked/markAllRevoked 后同步原子写(tmp+rename)且临界区重收集快照 | 代码审查 + 测试运行 | PASS | PluginDebugAuth.kt:145-166 三个写方法 `synchronized(persistLock){ delegate 写; persistLocked() }`；:207-215 persistLocked 临界区内 `delegate.snapshotTokens()` 重收集 → tmpFile.writeText → renameTo；测试 `persist is atomic…`（25 写无 tmp 残留）与 `concurrent writers never corrupt the file`（8×25 并发最终一致）通过 |
| 3 | load 损坏回退空不抛且过期记录丢弃并回写 | 代码审查 + 测试运行 | PASS | PluginDebugAuth.kt:179-204 load try/catch(Exception) → warn + emptyList，未知 version 抛 JSONException 同被捕获；expiresAt < now 的记录不入 delegate，expired 非空时 persistLocked 回写；测试 `corrupt file falls back…`/`unknown schema version…`/`expired token records are dropped and rewritten` 通过 |
| 4 | onAttachedToEngine applicationContext 幂等升级 processAuthStore 并迁移内存记录 | 代码审查 | PASS | DebugControlPlaneFlutterPlugin.kt:81-95：首次 attach 且 authUpgraded=false → upgradeProcessStore(binding.applicationContext)；:62-69 将原 InMemory 作为 FileBacked delegate（零拷贝迁移）+ persistNow() 落盘首写；@Synchronized + 类型判断双重幂等 |
| 5 | setAuthStoreForHost 整体替换语义不变 | 代码审查 | PASS | DebugControlPlaneFlutterPlugin.kt:72-78：仍整体替换 authStore 并重绑 pluginBridge，仅追加 `authUpgraded = true` 抑制后续升级，替换/桥重绑语义与 R004 前一致 |
| 6 | 单测覆盖 roundtrip/截断回退/可空往返/revoked 持久化/过期清理/原子写/并发 persist/红线 | 测试运行 | PASS | PluginDebugAuthStorePersistenceTest.kt 9 test 方法逐场景对应；evaluate 独立复跑 XML：tests=9 failures=0 errors=0；全套 79/0/0 |

### Scored Criteria

| 维度 | 分数(1-5) | 阈值 | 通过 | 说明 |
|------|----------|------|------|------|
| 功能完整性 | 5 | 3/5 | YES | 6 条验收全达标，3 项偏差（delegate 复用式迁移 / File 主构造 / filesDir 子目录）均为合理收敛且已在 Deviations 说明 |
| 设计质量 | 5 | 3/5 | YES | 装饰器职责单一、红线（pending 透传）结构化保证而非约定式；fail-soft 语义完整（load 回退空 / persist 失败不回滚内存） |
| 工艺 | 5 | 3/5 | YES | tmp+rename 原子写 + 临界区重收集快照 + 注入式 now/logger 可测性；simplify 主动删除死代码 migrateFrom 并补升级路径首写 flush |
| 并发安全 | 4 | 3/5 | YES | persistLock 串行化全部写路径、upgradeProcessStore @Synchronized；单进程假设已在 residual_risks 声明（当前架构成立） |
| 安全性 | 5 | 3/5 | YES | 明文 token/pairingCode/requestId 均不落盘，持久化仅 hash 记录；app-private filesDir 子目录 |
| 测试覆盖 | 5 | 3/5 | YES | 8 场景 9 用例，含并发（8 线程×25 写）与红线内容断言；TemporaryFolder 隔离 + 时钟注入 |

### 总分: 29/30 | 等级: A

### Findings

- [hard_gate] [low] 测试日志 frontmatter `passed: unknown` 且 Raw Output 为 `:kotlin:` 项目 `./gradlew build` 输出（config 命令源与实际 android 测试命令不匹配），日志本身不能直接证明 79 通过；evaluate 已独立复跑 `testDebugUnitTest` 核实 79/0/0，不阻塞本次，建议修正 `.dev-flow/config.json` 的 `tests.unit.android` 命令或 run-tests.sh 日志采集
- [hard_gate] [low] visual_verify_status: UNCERTAIN + DEFERRED 属预期状态，R004-BF002 需回收真机视觉证据与 `acceptance.auth.token_status_text` 稳定标识
- [scored_criteria] [low] residual risk 已知悉：同一 plugin 实例 setAuthStoreForHost 后 detach/attach 会回落 processAuthStore（host 每次注入语义，既有行为，非本 diff 引入）

### Next Action

- ready_for_commit（visual DEFERRED 债务由 R004-BF002 回收）

## Evaluate 结论（结构化）

```json
{
  "status": "pass",
  "grade": "A",
  "task_id": "R004-FF001",
  "domain": "ui",
  "mode": "negotiated",
  "hard_gates": {
    "domain_prefix_consistency": "PASS",
    "unit_test_pass": "PASS",
    "test_coverage": "PASS",
    "protocol_untouched": "PASS",
    "blocked_files_clean": "PASS",
    "plaintext_redline": "PASS",
    "attach_idempotent": "PASS",
    "simplify_pass": "PASS",
    "acceptance_criteria_met": "PASS",
    "visual_deferred_valid": "PASS"
  },
  "scored_criteria_results": [
    { "dimension": "功能完整性", "score": "5", "threshold": "3", "passed": "true", "reason": "6 条验收全达标，3 项偏差均为合理收敛并已登记" },
    { "dimension": "设计质量", "score": "5", "threshold": "3", "passed": "true", "reason": "装饰器 + 结构化红线 + 完整 fail-soft 语义" },
    { "dimension": "工艺", "score": "5", "threshold": "3", "passed": "true", "reason": "原子写/临界区快照/依赖注入可测性；simplify 收敛死代码并补首写 flush" },
    { "dimension": "并发安全", "score": "4", "threshold": "3", "passed": "true", "reason": "persistLock 全写路径串行 + @Synchronized 升级；单进程假设已声明" },
    { "dimension": "安全性", "score": "5", "threshold": "3", "passed": "true", "reason": "明文 token/pairingCode/requestId 不落盘，仅 hash 记录入 app-private 目录" },
    { "dimension": "测试覆盖", "score": "5", "threshold": "3", "passed": "true", "reason": "8 场景 9 用例含并发与红线内容断言，隔离与时钟注入完备" }
  ],
  "total_score": "29/30",
  "findings": [
    { "dimension": "hard_gate", "severity": "low", "description": "测试日志 passed=unknown 且 raw output 为 :kotlin: build 输出，evaluate 独立复跑核实 79/0/0；建议修正 config unit.android 命令源" },
    { "dimension": "hard_gate", "severity": "low", "description": "visual DEFERRED(device_required) 归 R004-BF002 回收，含 token_status_text 稳定标识" },
    { "dimension": "scored_criteria", "severity": "low", "description": "实例级 authUpgraded 在 host 注入后 re-attach 回落 processAuthStore，既有语义非本次引入" }
  ],
  "next_action": "ready_for_commit",
  "commit_ready": true
}
```

### Evaluate SubagentExecutionEvidence

- isolation: independent_context
- provider: direct_subagent
- subagent_role: xlfoundry-evaluate
- execution_id: xlf-subagent:R004-FF001:evaluate:01
