---
task_id: R004-FF002
requirement_cycle: R004
status: implemented
implementer_status: completed
simplify_status: completed
evaluate_status: pass
visual_verify_status: not_applicable
test_log_status: pass
commit_gate_status: unchecked
blocked_stage: null
blocked_reason: ""
domain: ui
subagent_required: true
isolation: direct_subagent
provider: direct_subagent
subagent_role: xlfoundry-implementer
execution_id: xlf-subagent:R004-FF002:implementer:01
implementer_execution_id: xlf-subagent:R004-FF002:implementer:01
---

# R004-FF002 Implementation Evidence

token TTL 默认 1h → 7 天（design §3.3 / DEC-R004-003）。纯常量改动：plugin 行为层全宿主免配置，1h 对自动化循环过短，7 天覆盖一个开发周。

## Files Changed

- `flutter_debug_control_plane/android/src/main/kotlin/com/pantas/debug/controlplane/flutter/PluginDebugAuth.kt`
  - `PluginDebugAuthManager.companion` 新增 `const val DEFAULT_TOKEN_TTL_SECONDS = 604800L`（simplify 阶段从 FileBackedPluginDebugAuthStore.companion 移回行为层 Owner，附 DEC-R004-003 注释）
  - `PluginDebugAuthManager.defaultTokenTtlSeconds` 默认值由字面量 `3600` 改为 `DEFAULT_TOKEN_TTL_SECONDS`
  - 唯一消费点不变：`ttlSeconds?.toLong() ?: defaultTokenTtlSeconds`（显式 ttlSeconds 宿主覆盖语义保持）
- `flutter_debug_control_plane/android/src/test/kotlin/com/pantas/debug/controlplane/flutter/PluginDebugAuthManagerTest.kt`
  - 新增 2 用例（见 Tests）

## Tests

- TDD 红：先写默认值用例后跑 `gradlew testDebugUnitTest --rerun-tasks` → `PluginDebugAuthManagerTest` failures=1（`expected default ttl near 604800s, got 3600`），确认红
- command: `bash scripts/run-tests.sh R004-FF002 unit android`
  - test override: `.dev-flow/R004/test-overrides/R004-FF002/unit-android.sh`（强制 `--rerun-tasks`，规避 Gradle UP-TO-DATE 假绿；任务包给的等价命令 `./gradlew build` 因增量缓存会跳过测试执行）
- log: `.dev-flow/R004/evidence/R004-FF002-unit-android-test.log`
- result: **pass — 81 tests, 0 failures, 0 errors**（PluginDebugAuthManagerTest 7 用例，新增 2）

新增用例：
1. `approve without ttl uses default 7d token ttl`：不注入 defaultTokenTtlSeconds、approve(ttlSeconds=null) → `expiresAt - now == DEFAULT_TOKEN_TTL_SECONDS`（simplify 收紧：原区间断言在受控时钟下属弱断言）
2. `approve with explicit ttl overrides default`：ttlSeconds=60 → `expiresAt - now == 60`

既有全部显式 ttl 用例（60/1s 过期等 5 个）零回归。

## Acceptance Check

- defaultTokenTtlSeconds 提常量 DEFAULT_TOKEN_TTL_SECONDS=604800 替换字面量 3600: passed
- 显式 ttlSeconds 通道语义不变（宿主覆盖默认）: passed
- 新增 JVM 回归用例断言不传 ttl 时 expiresAt-now == DEFAULT_TOKEN_TTL_SECONDS: passed
- 既有全部显式 ttl 用例零回归: passed

## Deviations

- 常量挂在 `FileBackedPluginDebugAuthStore.companion` 而非 Manager companion：codeSkeleton 建议 companion 常量但未指定归属类；文件内两个 companion 中 store 的为 private 常量组所在，挂此处并设 public 供 Manager 引用，语义即「plugin 默认 token TTL」。功能等价。（simplify 已收敛：移回 `PluginDebugAuthManager.companion`，见 Simplify Result）
- 新增 test override 脚本（`.dev-flow/R004/test-overrides/R004-FF002/unit-android.sh`）：config 中 unit.android 命令为 `./gradlew build`，增量缓存下测试 UP-TO-TO-DATE 直接假绿（本任务验证中实际踩到：旧 XML failures=1 但脚本报 pass）。override 是 run-tests.sh 的一等机制（scripts/run-tests.sh L225-232），非越权改 config。

## Visual Verify

not_applicable：纯常量改动（3600 → 604800），无 UI 表现、无新稳定标识、无交互变化；UI 面相关验收归 R004-BF002 真机端到端。

## Structured Summary

```json
{
  "task_id": "R004-FF002",
  "status": "success",
  "files_changed": [
    "flutter_debug_control_plane/android/src/main/kotlin/com/pantas/debug/controlplane/flutter/PluginDebugAuth.kt",
    "flutter_debug_control_plane/android/src/test/kotlin/com/pantas/debug/controlplane/flutter/PluginDebugAuthManagerTest.kt"
  ],
  "test_results": {"unit": "pass 81/81", "integration": "skipped (not declared in test_tasks)"},
  "evaluate_ready": true,
  "commit": null,
  "summary": "TTL 默认值提为 DEFAULT_TOKEN_TTL_SECONDS=604800L，红绿循环验证，新增默认值+显式覆盖 2 用例，81 测试全绿零回归"
}
```

## Simplify Result

execution_id: xlf-subagent:R004-FF002:simplify:01
isolation: direct_subagent
provider: direct_subagent
subagent_role: xlfoundry-simplify

- reviewed_files:
  - flutter_debug_control_plane/android/src/main/kotlin/com/pantas/debug/controlplane/flutter/PluginDebugAuth.kt
  - flutter_debug_control_plane/android/src/test/kotlin/com/pantas/debug/controlplane/flutter/PluginDebugAuthManagerTest.kt
  - .dev-flow/R004/test-overrides/R004-FF002/unit-android.sh
- fixed:
  - file: flutter_debug_control_plane/android/src/main/kotlin/com/pantas/debug/controlplane/flutter/PluginDebugAuth.kt
    description: DEFAULT_TOKEN_TTL_SECONDS 从 FileBackedPluginDebugAuthStore.companion 移至 PluginDebugAuthManager.companion（附 DEC-R004-003 注释），defaultTokenTtlSeconds 直接引用自身 companion 常量。TTL 默认值是 Manager 行为参数，挂 store 持久化层语义牵强且扩大 store 的 public 常量面。
  - file: flutter_debug_control_plane/android/src/test/kotlin/com/pantas/debug/controlplane/flutter/PluginDebugAuthManagerTest.kt
    description: 默认值用例断言由区间 [604790,604810] 收紧为 assertEquals(PluginDebugAuthManager.DEFAULT_TOKEN_TTL_SECONDS, ttl)。时钟由 now lambda 完全受控无漂移，区间断言属弱断言；同时消除魔法数字。
- routing: none
- bounced_to_implementer: []
- residual_risks:
  - test override 强制 --rerun-tasks 全量重跑（约 6s），代价可接受；根因是 config unit.android 命令 `./gradlew build` 的增量缓存假绿坑，已在 Deviations 记录，属 config 层问题，本任务不改。
- tests_after_fix: bash .dev-flow/R004/test-overrides/R004-FF002/unit-android.sh → BUILD SUCCESSFUL，PluginDebugAuthManagerTest 7 tests / 0 failures / 0 errors（全量 17 tasks executed）
- agents_summary:
  - agent_1_reuse: {issues_found: 0, issues_fixed: 0}
  - agent_2_quality: {issues_found: 2, issues_fixed: 2}
  - agent_3_efficiency: {issues_found: 0, issues_fixed: 0}
  - agent_4_architecture: {issues_found: 1, issues_fixed: 1, skipped: false}
  - agent_5_test: {issues_found: 1, issues_fixed: 1, bounced: false}
  - agent_6_design: {issues_found: 0, issues_fixed: 0, skipped: false}

说明：架构参考为 `.dev-flow/architecture.md`；红线 kotlin/、dart/、python/、PROTOCOL.md 零改动已核对（diff 仅触及 flutter plugin 行为层与测试）。test override 脚本经核对 scripts/run-tests.sh L225-232 的 OVERRIDE_TEST_SCRIPT 机制，路径与命名匹配 `${LAYER}-${DOMAIN}.sh`，内容最小（cd + gradlew testDebugUnitTest --rerun-tasks），正确。

## Sprint Evaluation
- task: R004-FF002
- domain: ui
- mode: negotiated
- status: pass
- grade: A
- execution_id: xlf-subagent:R004-FF002:evaluate:01
- isolation: direct_subagent
- provider: direct_subagent
- subagent_role: xlfoundry-evaluate

### Hard Gates
| Gate ID | 名称 | 结果 | 证据 |
|---------|------|------|------|
| domain_prefix_consistency | 前缀/domain 一致 | PASS | ui domain + FF 前缀，改动为 flutter plugin Android 行为层（与 R001-FF002/R004-FF001 同类归属），tasks.md 声明一致 |
| unit_test_pass | 单元测试通过 | PASS | R004-FF002-unit-android-test.log status=pass exit=0；XML 复核 81 tests / 0 failures / 0 errors；补跑 unit backend log status=pass 391 passed / 0 failed |
| test_coverage | 测试覆盖三维度 | PASS | 场景覆盖 2/2（default-7d / explicit-override 均有用例）；隔离：每用例新建 InMemoryPluginDebugAuthStore + FakeMethodChannel，无残留依赖；过程验证：断言 store 中 token 记录的 expiresAt 而非仅返回值 |
| protocol_untouched | wire 协议零改动 | PASS | git diff 仅 PluginDebugAuth.kt + 其测试，`/auth/*` 契约与 DebugAuthTokenRecord 结构未动 |
| blocked_files_clean | 红线文件零改动 | PASS | diff 未触及 kotlin/、dart/、python/、PROTOCOL.md |
| ttl_constant_correct | TTL 常量正确 | PASS | PluginDebugAuth.kt:284 `const val DEFAULT_TOKEN_TTL_SECONDS = 604800L`；:279 默认值引用常量，仓库内已无 3600 字面量残留 |
| explicit_override_preserved | 显式覆盖语义不变 | PASS | PluginDebugAuth.kt:374 `ttlSeconds?.toLong() ?: defaultTokenTtlSeconds` 消费点未变；新增 explicit-override 用例断言 ttl=60 → expiresAt-now==60 |
| simplify_pass | simplify 完成 | PASS | Simplify Result：常量归属收敛至 PluginDebugAuthManager.companion（DEC-R004-003），断言收紧为 assertEquals(常量)；agent_2/4/5 共 4 项全部修复；simplify 后 81 tests 复跑全绿 |
| acceptance_criteria_met | 验收标准逐条校验 | PASS | 见下方矩阵 |

### Acceptance Criteria Checklist
| # | acceptance_criteria | 验证方式 | 结果 | 证据 |
|---|-------------------|---------|------|------|
| 1 | defaultTokenTtlSeconds 提常量 DEFAULT_TOKEN_TTL_SECONDS=604800 替换字面量 3600 | 代码审查 | PASS | PluginDebugAuth.kt:279,284；companion 常量=604800L，默认参数引用之 |
| 2 | 显式 ttlSeconds 通道语义不变(宿主覆盖默认) | 代码审查 + 测试运行 | PASS | PluginDebugAuth.kt:374 消费点未改；测试 `approve with explicit ttl overrides default` 断言 60s 生效 |
| 3 | 新增 JVM 回归用例断言不传 ttl 时 expiresAt-now ∈ [604790,604810] | 测试运行（语义等价） | PASS | `approve without ttl uses default 7d token ttl`：受控时钟下 assertEquals(DEFAULT_TOKEN_TTL_SECONDS, ttl)，精确断言强于区间断言，语义等价覆盖 |
| 4 | 既有全部显式 ttl 用例零回归 | 测试运行 | PASS | testDebugUnitTest 81/81 绿（PluginDebugAuthManagerTest 7 用例含既有显式 ttl 用例）；XML 复核 failures=0 errors=0 |

### Scored Criteria
| 维度 | 分数(1-5) | 阈值 | 通过 | 说明 |
|------|----------|------|------|------|
| 功能完整性 | 5 | 3 | YES | 4 条 AC 全达标，默认值与覆盖通道双验证 |
| 工艺 | 5 | 3 | YES | 常量归属行为层 Owner、注释引 DEC、断言消除魔法数字 |
| 测试覆盖 | 5 | 3 | YES | TDD 红（旧默认 3600 时红）→ 绿；场景/隔离/过程三维度达标 |
| 平台适配 | 5 | 3 | YES | 纯 JVM 常量改动，Android 宿主零额外配置 |

### 总分: 20/20 | 等级: A

### Findings
- [hard_gate] [low] unit-android log frontmatter `passed: unknown`（run-tests.sh 解析 Gradle 输出限制）；已独立复核 XML 81/0/0，不阻塞
- [process] [low] TDD 红日志（R004-FF002-red-unit-android-test.log）实为 config 命令 `./gradlew build` 增量缓存下的假绿样例（红证据来自 XML failures=1），该坑已由 test override（--rerun-tasks）修复并记录 Deviations；config 层假绿问题留待后续任务
- [process] [low] evaluate 补跑 unit backend 日志（391 passed）供 commit gate，非 test_tasks 声明项，纯回归保障

### Next Action
- ready_for_commit
