---
task_id: R006-BF001
requirement_cycle: R006
status: implemented
implementer_status: completed
simplify_status: completed
evaluate_status: pass
visual_verify_status: not_applicable
test_log_status: pass
domain: backend
subagent_required: true
isolation: direct_subagent
provider: direct_subagent
subagent_role: xlfoundry-implementer
execution_id: xlf-subagent:R006:R006-BF001:implementer:01
implementer_execution_id: xlf-subagent:R006:R006-BF001:implementer:01
simplify_execution_id: xlf-subagent:R006:R006-BF001:simplify:01
evaluate_execution_id: xlf-subagent:R006:R006-BF001:evaluate:01
blocked_stage: null
blocked_reason: ""
---

# R006-BF001 Implementer Evidence

## Implementation Summary

- `ChannelProtocol.kt`：PlatformException codes 节新增 `ERROR_INVALID_ARGUMENTS = "invalid_arguments"`；新增 authPolicy 节——`AUTH_POLICY = "authPolicy"` 参数名 + `AUTH_POLICY_DEFAULT/AUTO/NONE`（"default"/"auto"/"none"）+ `AUTH_POLICY_VALUES` 合法值集合。既有常量零改动（ChannelProtocolAlignmentTest 守护）。
- `PluginDebugAuth.kt`：构造追加 `autoApprove: Boolean = false`（默认值，既有调用点零破坏）；`requestAuthorization` 在 `bridge.requestAuthorization(...)` 审计通知后、return 前插入 auto 分支——`pending.status == "pending"` 守卫（nonce 重放幂等：existing 已 approved 直接返回现状，不再 approve 抛 IAE）→ 调既有 `approve(requestId, ttlSeconds = null, clientLabel = pending.clientLabel)` → store 重读 pending 返回（202 status=approved）。审计通知在 auto 下仍发（design D4）。
- `DebugControlPlaneFlutterPlugin.kt`：`PLANE_START` handler 读可选 `authPolicy`（缺席=default），非法值在 `pluginScope.launch` **之前** `result.error(invalid_arguments)` 且 return——plane 不启动不静默回退（D5）；`ensurePlane` 加第四参数：JOIN 分支（PlaneCarrier.plane 非空）只 updateAppMeta 不重建（策略 start 后不可变）；fallback 分支 `when(authPolicy)` 三分支——none→`mount(transport, scope, null)`、auto→重建 `PluginDebugAuthManager(bridge, authStore, autoApprove=true)`、default→既有 attach 时 authManager。
- `PluginAuthPolicyTest.kt`（新建）：K1-K8 八用例，复用 BridgeTestDoubles（FakeMethodChannel/FakeTransport/InMemoryPluginDebugAuthStore）+ 反射注入模式 + `PlaneCarrier.unmount()` @After 清理。

## Files Changed

- flutter_debug_control_plane/android/src/main/kotlin/com/pantas/debug/controlplane/flutter/ChannelProtocol.kt（修改）
- flutter_debug_control_plane/android/src/main/kotlin/com/pantas/debug/controlplane/flutter/PluginDebugAuth.kt（修改）
- flutter_debug_control_plane/android/src/main/kotlin/com/pantas/debug/controlplane/flutter/DebugControlPlaneFlutterPlugin.kt（修改）
- flutter_debug_control_plane/android/src/test/kotlin/com/pantas/debug/controlplane/flutter/PluginAuthPolicyTest.kt（新建）

## Tests

- commands_run: `cd flutter_debug_control_plane/android && ../../kotlin/gradlew -p . testDebugUnitTest -q`
- unit: passed（89 tests / 0 failed / 0 skipped；PluginAuthPolicyTest 新增 8 用例；ChannelProtocolAlignmentTest / PluginDebugAuthManagerTest / DebugControlPlaneFlutterPluginTest / PluginOwnershipTest 等既有零回归）

## 偏差与决策

1. approve 幂等守卫：nonce 重放命中已 approved existing 时跳过 approve 直接返回（防 IAE "request is not pending"），K6 子断言覆盖。
2. K3 断言类型修正：plane 层 dispatch 返回 RouteResult.Ok（auth 路由结果被包成通用 RouteResult），首轮 ClassCastException 后改断言类型。
3. auto 分支 bridge null → IllegalStateException → internal_error（attach 语义上 bridge 不应为 null）。

## Simplify 结论

- PASS（无需改，0 处修改）：三分支 when 收敛、守卫可达性验证、测试基建复用、断言直击行为、范围无混入。1 条排版建议（authPolicy 分节位置）不阻断。

## Evaluate 结论

- verdict: PASS（ready_for_commit）
- acceptance_criteria 10/10 PASS（含任务模板未明列的 JOIN 不重建语义）
- 亲自重跑测试门：89/0/0
- 红线核查：blocked_files 零触碰；wire 常量 "default"/"auto"/"none"/"invalid_arguments" 与 design §4.1 逐字一致
- 评分：需求覆盖 5 / 测试有效性 5 / 代码质量 5 / 红线遵守 5
