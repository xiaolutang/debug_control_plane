---
task_id: R005-BF002
requirement_cycle: R005
status: implemented
implementer_status: completed
simplify_status: completed
evaluate_status: pass
visual_verify_status: not_applicable
test_log_status: pass
domain: integration
subagent_required: true
isolation: direct_subagent
provider: direct_subagent
subagent_role: xlfoundry-implementer
execution_id: xlf-subagent:R005-BF002:implementer:01
implementer_execution_id: xlf-subagent:R005-BF002:implementer:01
blocked_stage: null
blocked_reason: ""
---

# R005-BF002 Implementer Evidence

## Implementation Summary

- `run-integration.sh`（176 行驱动）：simctl 检测（booted→可用→DEFERRED 三态）→ `fvm flutter build ios --simulator --debug` → simctl install → driver 首启（fork 的 r005_persistence_driver_test.dart，DRIVER_SECONDS 存活）→ endpoint 发现（driver stdout `pytest-driver: endpoint=`）→ python 断言脚本主控 → evidence 双写（deferred 契约 device_required 白名单）。
- `ios-simulator-persistence.py`（388 行）：python 主控 I1-I5，含 simctl terminate/relaunch 冷重启（I2/I3/I4 各一次）；app 沙箱路径 `simctl get_app_container <udid> <bundle> data` + `Documents/debug_control_plane/debug_auth_tokens.json`。
- `test_dart_plane_persistence.py`（218 行 pytest 入口）：endpoint 双通道（--endpoint/ACCEPTANCE_ENDPOINT）；session fixture skip 模式（对齐 test_acceptance_flutter_app_auth.py）；I1/I4-TTL/I5 子集用例；I2/I3 docstring 明示仅驱动脚本执行。
- `r005_persistence_driver_test.dart`（71 行）：fork 自 acceptance_pytest_driver_test.dart + `ensurePersistentStore` 挂载（R005-FF001 装配层语义）；clientLabel 前缀分派 deny*/hold*/其他→approve。

## Files Changed

- .dev-flow/R005/test-overrides/R005-BF002/run-integration.sh（新增）
- .dev-flow/R005/test-overrides/R005-BF002/ios-simulator-persistence.py（新增）
- python/tests/test_dart_plane_persistence.py（新增）
- flutter_debug_control_plane/example/integration_test/r005_persistence_driver_test.dart（新增）
- .gitignore（simplify：+ .work/ 与 .android-work/ 忽略规则）

## Tests

- integration: **重跑复现 PASS**（编排者现场二次执行，iPhone 16e iOS 18.5 模拟器）——I1-I5 全 pass，IOS_E2E_STATUS: pass；日志 evidence/R005-BF002-integration-cross_stack-test.log（run_at 2026-09-01T10:21:09Z）
- unit（python 全量）: 391 passed, 13 skipped, **0 error**（含本任务 6 用例无 endpoint 场景 skip 不误报）
- 关键输出：
  - I2 PASS: 冷重启(terminate+relaunch)后旧 Bearer /hello 200 authStatus=authorized;敏感路由 200;零 /auth/request
  - I4 PASS: TTL 窗口 604800s(∈[604790,604810]);过期行重启→401 invalid_token(load-discard)

## Acceptance Check（evaluate 独立复核 8/8 PASS）

1. 驱动 build+install+launch+endpoint 复用 R004 模式: PASS
2. I1 双侧落盘+沙箱字节无明文（:239 token.encode() not in raw）: PASS
3. I2 冷重启主断言 200 authorized: PASS（:256）
4. I3 损坏自愈: PASS（:270-286）
5. I4 TTL 窗口 604790-604810: PASS（:294）
6. I5 wire 回归: PASS（:317-339）
7. deferred 契约: PASS（run-integration.sh:43-66/:73-77）
8. pytest 全量零回归: PASS（391 passed 0 error）

## Deviations（已批准/编排者处理）

1. **I4 过期断言码 invalid_token（非 token_expired）**：BF001 load 时过期行丢弃→record 不存在→invalid_token。load-discard 语义等价（401 + 生命周期终止），脚本 :304-305 注释说明。AC 原文「token_expired」按实际实现语义判 PASS（evaluate 复核确认）。
2. **pytest fixture skip 模式修复（编排者）**：初版 session fixture 抛 _EndpointUnreachable 导致 6 用例 ERROR 而非 SKIP；改为 session 内直接 pytest.skip（对齐 R002-BF005 模式），391 passed 13 skipped 0 error。
3. 断言脚本硬编码 REPO 绝对路径（evaluate low 备注，非阻塞，沿用 R004 先例）。

## Simplify Result

- verdict: modified（仅 .gitignore +2 条运行时工作目录忽略规则；断言脚本/driver/pytest/驱动脚本零改动）
- 跨文件工具函数重复（http_json/wait_approved 双实现）按判据接受：两种运行模式（subprocess import 包 vs 纯标准库），R004 先例同构。
- 复验: pytest 391 passed 13 skipped 零 error ×2。

### SubagentExecutionEvidence
- isolation: direct_subagent
- provider: direct_subagent
- subagent_role: xlfoundry-simplify
- execution_id: xlf-subagent:R005-BF002:simplify:01

## Sprint Evaluation
- task: R005-BF002
- domain: integration
- taskLayer: acceptance
- mode: direct
- status: pass
- execution_id: xlf-subagent:R005-BF002:evaluate:01

### 现场复验（evaluate 实跑）
- evidence log frontmatter status=pass + I1-I5 全 pass
- pytest tests -q: 391 passed 13 skipped 0 error
- test_dart_plane_persistence.py -q: 6 skipped 不误报
- 抽查断言：I2 authStatus authorized（:256）/明文红线（:239/:282）/TTL 窗口（:294）
- blocked_files diff（dart/lib kotlin python/debug_control_plane example/lib plugin lib）: 空

### SubagentExecutionEvidence
- isolation: direct_subagent
- provider: direct_subagent
- subagent_role: xlfoundry-evaluate
- execution_id: xlf-subagent:R005-BF002:evaluate:01

## Next Action
- ready_for_commit
