---
task_id: R005-BF001
requirement_cycle: R005
status: implemented
implementer_status: completed
simplify_status: completed
evaluate_status: pass
visual_verify_status: not_applicable
test_log_status: pass
domain: backend
subagent_required: true
isolation: direct_subagent
provider: direct_subagent
subagent_role: xlfoundry-implementer
execution_id: xlf-subagent:R005-BF001:implementer:01
implementer_execution_id: xlf-subagent:R005-BF001:implementer:01
blocked_stage: null
blocked_reason: ""
---

# R005-BF001 Implementer Evidence

## Implementation Summary

- 新建 `dart/lib/src/debug_auth_store.dart`：
  - `TokenRecord`（tokenId/tokenHash/createdAt/expiresAt/revokedAt/clientLabel，Kotlin `DebugAuthTokenRecord` 同构；toJson/fromJson ISO8601 UTC 往返）。
  - `DebugAuthStore` 抽象恰好 5 方法（tokenByHash/putToken/markRevoked/markAllRevoked/removeExpired）。
  - `InMemoryDebugAuthStore`：map 键=tokenHash（红线）；`snapshot()` 供 persist 枚举（Kotlin snapshotTokens 同构）。
  - `FileBackedDebugAuthStore` 装饰器：构造不读盘（惰性 `_ensureLoaded`）；损坏/非 Map/version≠1 回退空不抛（fail-soft）；过期行 load 丢弃且**不回写**（与 Kotlin 差异点，AC 明确）；写方法先 delegate 后同步 `_persist()`（tmp 写入 flush + renameSync 原子替换，无 .tmp 残留，全程 fail-soft）。
  - sha256 纯 Dart FIPS 180-4（K 表 64 常量 + 64 轮 + padding），UTF-8 输入，64 位小写 hex；`_sha256Hex` 私有 + `debugAuthTokenHash` 公开别名。
- `dart/lib/debug_control_plane.dart`：+1 行 `export 'src/debug_auth_store.dart';`。
- 新建 `dart/test/debug_auth_store_test.dart` U1-U10（临时目录隔离，tearDown 清理）。

## Files Changed

- dart/lib/src/debug_auth_store.dart（新增）
- dart/lib/debug_control_plane.dart（+1 行 export）
- dart/test/debug_auth_store_test.dart（新增，10 用例）

## Tests

- unit: `cd dart && flutter test` → **+129: All tests passed!**（既有零回归 + U1-U10 全过）
- analyze: `flutter analyze` → No issues found
- integration: skipped（本任务 test_tasks 仅 unit；integration 属 R005-BF002）
- 日志：evidence/R005-BF001-unit-backend-test.log

## Acceptance Check

- TokenRecord 与 Kotlin 字段同构: passed
- InMemory map 键=tokenHash: passed
- FileBacked 惰性 load + 损坏/version 不符回退不抛: passed（U4 三变体）
- 写方法同步 persist + tmp+rename 原子 + 无残留: passed（U8）
- load 过期丢弃不回写: passed（U5 断言文件字节未变）
- sha256 NIST 向量 abc/空串: passed（U2 另加 UTF-8 中文向量）
- 覆盖 roundtrip/损坏/过期/原子/明文红线: passed（U1/U4/U5+U7/U8/U9）
- dart pubspec.yaml 零新依赖: passed（git diff 为空）
- 既有 dart/test 零回归: passed（129 全绿）

## Deviations

1. **DEC-R005-002 微修正（编排者批准）**：设计原表述 sha256「库内私有不导出」；实现为 `_sha256Hex` 私有 + `debugAuthTokenHash()` 公开顶层别名——FF001 example（禁改 dart/lib）校验路径需要公开 hash 入口，Kotlin `DebugAuth.tokenHash` 公开同构；零新依赖意图不变。
2. **中文 sha256 向量值勘误**：任务包预计算值有笔误，实现时以 `python3 hashlib` 实测为准（`670d9743...711302e`）。

## Simplify Result

- verdict: modified
- reviewed_files:
  - dart/lib/src/debug_auth_store.dart
  - dart/lib/debug_control_plane.dart
  - dart/test/debug_auth_store_test.dart
- fixed（仅 InMemoryDebugAuthStore，API 面未动）:
  - markRevoked：indexWhere+elementAt 两次全表扫 + 手写 7 字段拷贝 → firstOrNull 单次遍历 + 共享 `_withRevoked` 拷贝 helper
  - markAllRevoked：逐条委托（O(n²)）→ `_tokens.updateAll(...)` 直接重建
- 未触碰：API 规格/sha256 手写/Kotlin 同构结构/fail-soft try-catch/U1-U10
- 复验: flutter test 129 全绿 + analyze No issues

### SubagentExecutionEvidence
- isolation: independent_context
- provider: direct_subagent
- subagent_role: xlfoundry-simplify
- execution_id: xlf-subagent:R005-BF001:simplify:01

## Sprint Evaluation
- task: R005-BF001
- domain: backend
- taskLayer: foundation
- mode: direct
- status: pass
- execution_id: xlf-subagent:R005-BF001:evaluate:01

### Acceptance Criteria Checklist（evaluate 独立复核）

| # | AC | 结果 | 证据 |
|---|---|---|---|
| 1 | TokenRecord 与 Kotlin 同构 | PASS | debug_auth_store.dart:19-34 |
| 2 | InMemory 键=tokenHash | PASS | :73-115 `_tokens[record.tokenHash]` |
| 3 | 惰性 load 损坏回退不抛 | PASS | `_ensureLoaded()` :140-166 + U4 |
| 4 | 同步 persist tmp+rename 无残留 | PASS | :175-210 + U8 |
| 5 | 过期丢弃不回写 | PASS | :160 + U5 |
| 6 | sha256 NIST 向量 | PASS | U2 三向量 |
| 7 | 单测五类场景覆盖 | PASS | U1/U4/U5+U7/U8/U9，9 scenario 全对应用例 |
| 8 | pubspec 零新依赖 | PASS | git diff 空 |
| 9 | 既有零回归 | PASS | +129 All tests passed |

### Basic Gates
- unit_test_pass: PASS（+129）
- test_coverage: PASS（9/9 scenario）
- integration_test_pass: N/A（属 BF002）
- no_new_lint_errors: PASS（analyze No issues）
- simplify_completed: PASS（1 处收敛，复验绿）
- 红线抽查: map 键=hash、persist payload 无明文、debugAuthTokenHash 为批准的公开别名

### SubagentExecutionEvidence
- isolation: independent_context
- provider: direct_subagent
- subagent_role: xlfoundry-evaluate
- execution_id: xlf-subagent:R005-BF001:evaluate:01

## Next Action
- ready_for_commit
