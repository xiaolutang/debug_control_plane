---
task_id: R005-FF001
requirement_cycle: R005
status: implemented
implementer_status: completed
simplify_status: completed
evaluate_status: pass
visual_verify_status: not_applicable
test_log_status: pass
domain: ui
subagent_required: true
isolation: direct_subagent
provider: direct_subagent
subagent_role: xlfoundry-implementer
execution_id: xlf-subagent:R005-FF001:implementer:01
implementer_execution_id: xlf-subagent:R005-FF001:implementer:01
blocked_stage: null
blocked_reason: ""
---

# R005-FF001 Implementer Evidence

## Implementation Summary

- `acceptance_plane.dart`：
  - `AcceptanceDebugAuthManager` 构造 `+DebugAuthStore? store`（缺省 InMemoryDebugAuthStore）、`tokenTtl` 默认 15min → `Duration(days: 7)`；删除 `_tokens` 明文 map 与 `_IssuedToken` 类。
  - authorize（:279）/helloAuthState（:312）改走 `_store.tokenByHash(debugAuthTokenHash(token))`；判定序 invalid → revoked → expired（revoked 分支为 store 化后的语义自然补全，行为超集不破坏 wire）。
  - claim `putToken(TokenRecord(...))`（:445），wire 响应体逐字段不变。
  - clearToken → `_store.markAllRevoked(_now())`；expireToken → `_expiryOverrides` 内存旁路（DEC-R005-005，tokenHash→now-1s 不持久化），authorize 判 `min(record.expiresAt, override)`。
  - `AcceptancePlane.ensurePersistentStore()`：getApplicationDocumentsDirectory → `<docs>/debug_control_plane` → FileBackedDebugAuthStore → swapStore 迁移（InMemory snapshot 全量 put 后 identical 早退）；幂等 + fail-soft。
  - pending 组逻辑零改动（D6）。
- `main.dart`（DEC-R005-006）：`WidgetsFlutterBinding.ensureInitialized()` → `await controller.ensurePersistentStore()`（controller→DartPlaneHost→plane 透传，Android 原生宿主 no-op）→ runApp。
- `pubspec.yaml`：+`path_provider: ^2.1.0`。
- `test/acceptance_plane_test.dart`：既有 4 用例零改动全绿；新增 group('R005 token persistence') E1/E2/E3/E5（E4=全文件零回归，注释显式声明）。

## Files Changed

- flutter_debug_control_plane/example/lib/src/acceptance_plane.dart（核心改造）
- flutter_debug_control_plane/example/pubspec.yaml（+path_provider）
- flutter_debug_control_plane/example/test/acceptance_plane_test.dart（+E1-E5）
- flutter_debug_control_plane/example/lib/main.dart（装配层挂载）
- flutter_debug_control_plane/example/lib/src/acceptance_controller.dart（+ensurePersistentStore 透传，纯增量）

## Tests

- unit: `cd flutter_debug_control_plane/example && flutter test` → **+54: All tests passed!**（含 R002-FF004 plane_started 恢复 + E1-E5）
- analyze: 仅基线 1 条 unused_element_parameter（:605，改动前已存在于 :515），无新增
- 日志：evidence/R005-FF001-unit-flutter-test.log

## Acceptance Check（evaluate 独立复核 8/8 PASS）

- 构造 store 缺省 InMemory + TTL 默认 7d: PASS（:123-133）
- 装配层默认 FileBacked（DEC-R005-006 main() 落点）: PASS（acceptance_plane.dart:40-60 + main.dart:21）
- 校验路径 hash 化两处: PASS（:279/:312）
- 明文 map/_IssuedToken 删除: PASS（grep 零命中）
- claim TTL 604790-604810s: PASS（E3 :280-296 固定时钟断言）
- clear/expire 经 store 且公开 API 不变: PASS（:211/旁路/:151-155）
- 既有零回归 + E1-E5: PASS（+54 全绿）
- wire 零改动: PASS（diff 未触路由 handler；既有断言原值通过）

## Deviations（均已批准）

1. **DEC-R005-005**：expireToken 用 `_expiryOverrides` 内存旁路（TokenRecord 不可变，无法原地过期；markRevoked 会混淆 expired/revoked 语义）。仅内存不持久化，doc comment 注明 UI 演示便利。
2. **DEC-R005-006**：持久化挂载点从 startDartPlane（tasks 原案）移到 main() 装配层——widget 测试 fake-async 无引擎插件，path_provider reply 永不完成，startDartPlane 内 await 实测挂起（R002-FF004 用例失败复现）。widget 测试不执行 main() 零影响；真机/模拟器 main() 先跑，claim 前 FileBacked 已挂载，I2 冷重启语义保持。
3. acceptance_controller.dart 超出 tasks 文件清单：DEC-R005-006 接线必需的透传方法，纯增量 10 行。

## Simplify Result

- verdict: modified
- fixed：删除测试内自造泛型扩展 `_Let<T>.let`，替换为语义明确的顶层 helper `DateTime Function() _fixedClock(DateTime at)`（E1/E5 now 参数改写；用例/断言/行为零改动）。
- 其余：冻结契约/已批准决策/E1-E5 均未动；无诊断残留（grep _diag/print 干净）；ensurePersistentStore 幂等 + swapStore identical 早退清晰。
- 复验: flutter test 54 全绿 + analyze 无新增。

### SubagentExecutionEvidence
- isolation: direct_subagent
- provider: direct_subagent
- subagent_role: xlfoundry-simplify
- execution_id: xlf-subagent:R005-FF001:simplify:01

## Sprint Evaluation
- task: R005-FF001
- domain: ui
- taskLayer: foundation
- mode: direct
- status: pass
- execution_id: xlf-subagent:R005-FF001:evaluate:01

### 现场复验（evaluate 实跑）

| 复验项 | 结果 |
|---|---|
| flutter test | +54 All tests passed |
| flutter analyze | 仅基线 1 条，无新增 |
| blocked_files diff（dart/lib kotlin python PROTOCOL.md plugin lib） | 空 |
| 明文 map grep | 零命中 |
| hash 查询两处 | :279/:312 |
| TTL 7d 默认 | :125 |
| main() 装配 | main.dart:16-23 |
| E3 区间断言 | test:280-296 |

### SubagentExecutionEvidence
- isolation: direct_subagent
- provider: direct_subagent
- subagent_role: xlfoundry-evaluate
- execution_id: xlf-subagent:R005-FF001:evaluate:01

## Next Action
- ready_for_commit
