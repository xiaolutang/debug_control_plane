name: debug_control_plane_acceptance_example
description: Acceptance fixture app for debug_control_plane auth flows.
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.0.0'

dependencies:
  flutter:
    sdk: flutter
  debug_control_plane:
    path: ../../dart
  flutter_debug_control_plane:
    path: ../
  path_provider: ^2.1.0

dependency_overrides:
  debug_control_plane:
    path: ../../dart

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  flutter_lints: ^6.0.0
